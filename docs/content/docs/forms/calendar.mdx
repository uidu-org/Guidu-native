---
title: "Calendar"
description: a great way to use calendars
---

import { Button } from "@holo/button";
import CalendarDemo from "../../../src/docs/demos/forms/calendar-demo.tsx";

    <div className="flex items-center">
        <Button variant="link" className="mr-4"><a className="!no-underline" href="https://www.npmjs.com/package/@holo/calendar" target="_blank">@holo/calendar</a></Button>
        <Button variant="link" className="mx-4"><a className="!no-underline"  href="https://github.com/uidu-org/holo/tree/main/packages/components/forms/calendar" target="_blank">Source</a></Button>
    </div>

## Calendar

<CalendarDemo />

## Usage

    <Tabs items={["preview" , "code"]}>
        <Tab value="preview">
            <div className="py-16 flex items-center justify-center">
                <CalendarDemo />
            </div>
        </Tab>
        <Tab value="code">
            ```tsx
                "use client";

                import * as React from "react";

                import { Calendar } from "@holo/forms";

                export default function CalendarDemo() {
                const [date, setDate] = React.useState<Date | undefined>(new Date());

                return (
                    <Calendar
                    mode="single"
                    selected={date}
                    onSelect={setDate}
                    className="rounded-md border shadow"
                    />
                );
                }
            ```
        </Tab>
    </Tabs>

## Installation

    <Tabs items={["npm" , "yarn"]}>
        <Tab value="npm">
            ```bash
                npm install @holo/calendar
            ```
        </Tab>
        <Tab value="yarn">
            ```bash
                yarn add @holo/calendar
            ```
        </Tab>
    </Tabs>

## Import

        <Tabs items={["generic" , "individual"]}>
        <Tab value="generic">
            ```tsx
                import { Calendar } from "@holo/forms";
            ```
        </Tab>
        <Tab value="individual">
            ```tsx
                import { Calendar } from "@holo/calendar";
            ```
        </Tab>
    </Tabs>
