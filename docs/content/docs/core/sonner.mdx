---
title: "Sonner"
description: a great way to use sonners!
---

import { Button } from "@holo/button";
import SonnerDemo from "../../../src/docs/demos/core/sonner-demo";

    <div className="flex items-center">
        <Button variant="link" className="mr-4"><a className="!no-underline" href="" target="_blank">Storybook</a></Button>
        <Button variant="link" className="mr-4"><a className="!no-underline" href="" target="_blank">@holo/TBD</a></Button>
        <Button variant="link" className="mx-4"><a className="!no-underline"  href="" target="_blank">Source TBD</a></Button>
    </div>

## Sonner

<SonnerDemo />

## Usage

    <Tabs items={["preview" , "code"]}>
        <Tab value="preview">
            <div className="py-16 flex items-center justify-center">
                <SonnerDemo />
            </div>
        </Tab>
        <Tab value="code">
            ```tsx
                "use client"

                import { toast } from "sonner"

                import { Button } from "@holo/core"

                export default function SonnerDemo() {
                  return (
                    <Button
                      variant="outline"
                      onClick={() =>
                        toast("Event has been created", {
                          description: "Sunday, December 03, 2023 at 9:00 AM",
                          action: {
                            label: "Undo",
                            onClick: () => console.log("Undo"),
                          },
                        })
                      }
                    >
                      Show Toast
                    </Button>
                  )
                }
            ```
        </Tab>
    </Tabs>

## Installation

    <Tabs items={["npm" , "yarn"]}>
        <Tab value="npm">
            ```bash
                npm install sonner @holo/button
            ```
        </Tab>
        <Tab value="yarn">
            ```bash
                yarn add sonner @holo/button
            ```
        </Tab>
    </Tabs>

## Import

```tsx
import { toast } from "sonner";

import { Button } from "@holo/core";
```
