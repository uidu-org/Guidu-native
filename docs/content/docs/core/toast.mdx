---
title: "Toast"
description: a great way to use toasts!
---

import { Button } from "@holo/button";
import ToastDemo from "../../../src/docs/demos/core/toast-demo";
import ToastDestructive from "../../../src/docs/demos/core/toast-destructive.tsx";
import ToastSimple from "../../../src/docs/demos/core/toast-simple.tsx";
import ToastWithAction from "../../../src/docs/demos/core/toast-with-action.tsx";
import ToastWithTitle from "../../../src/docs/demos/core/toast-with-title.tsx";

    <div className = "flex items-center">
        <Button variant="link" className="mr-4"><a className="!no-underline" href="" target="_blank">Storybook</a></Button>
    </div>
    <div className="flex items-center">
        <Button variant="link" className="mr-4"><a className="!no-underline" href="https://www.npmjs.com/package/@holo/toast" target="_blank">@holo/toast</a></Button>
        <Button variant="link" className="mx-4"><a className="!no-underline"  href="https://github.com/uidu-org/holo/tree/main/packages/components/core/toast" target="_blank">Source</a></Button>
    </div>

## Toast

<ToastDemo />

## Usage

    <Tabs items={["preview" , "code"]}>
        <Tab value="preview">
            <div className="py-16 flex items-center justify-center">
                <ToastDemo />
            </div>
        </Tab>
        <Tab value="code">
            ```tsx
                "use client"

                import { Button, ToastAction, useToast } from "@holo/core"

                export default function ToastDemo() {
                  const { toast } = useToast()

                  return (
                    <Button
                      variant="outline"
                      onClick={() => {
                        toast({
                          title: "Scheduled: Catch up ",
                          description: "Friday, February 10, 2023 at 5:57 PM",
                          action: (
                            <ToastAction altText="Goto schedule to undo">Undo</ToastAction>
                          ),
                        })
                      }}
                    >
                      Add to calendar
                    </Button>
                  )
                }
            ```
        </Tab>
    </Tabs>

## Installation

    <Tabs items={["npm" , "yarn"]}>
        <Tab value="npm">
            ```bash
                npm install @holo/toast @holo/button
            ```
        </Tab>
        <Tab value="yarn">
            ```bash
                yarn add @holo/toast @holo/button
            ```
        </Tab>
    </Tabs>

## Import

    <Tabs items={["generic" , "individual"]}>
        <Tab value="generic">
            ```tsx
                import { Button, ToastAction, useToast } from "@holo/core"
            ```
        </Tab>
        <Tab value="individual">
            ```tsx
                import { Button } from "@holo/button"
                import { ToastAction, useToast } from "@holo/toast"
            ```
        </Tab>
    </Tabs>

## Variants

    <Steps>
        <Step>
          ### Destructive
            <Tabs items={["preview" , "code"]}>
                <Tab value="preview">
                    <div className="py-16 flex items-center justify-center">
                        <ToastDestructive />
                    </div>
                </Tab>
                <Tab value="code">
                    ```tsx
                        "use client"

                        import { Button, ToastAction, useToast } from "@holo/core"

                        export default function ToastDestructive() {
                          const { toast } = useToast()

                          return (
                            <Button
                              variant="outline"
                              onClick={() => {
                                toast({
                                  variant: "destructive",
                                  title: "Uh oh! Something went wrong.",
                                  description: "There was a problem with your request.",
                                  action: <ToastAction altText="Try again">Try again</ToastAction>,
                                })
                              }}
                            >
                              Show Toast
                            </Button>
                          )
                        }
                    ```
                </Tab>
            </Tabs>
        </Step>
        <Step>
          ### Simple
                <Tabs items={["preview" , "code"]}>
                    <Tab value="preview">
                        <div className="py-16 flex items-center justify-center">
                            <ToastSimple />
                        </div>
                    </Tab>
                    <Tab value="code">
                        ```tsx
                           "use client"

                            import { Button, useToast } from "@holo/core"

                            export default function ToastSimple() {
                              const { toast } = useToast()

                              return (
                                <Button
                                  variant="outline"
                                  onClick={() => {
                                    toast({
                                      description: "Your message has been sent.",
                                    })
                                  }}
                                >
                                  Show Toast
                                </Button>
                              )
                            }
                        ```
                    </Tab>
                </Tabs>
        </Step>
        <Step>
          ### With Action
                <Tabs items={["preview" , "code"]}>
                    <Tab value="preview">
                        <div className="py-16 flex items-center justify-center">
                            <ToastWithAction />
                        </div>
                    </Tab>
                    <Tab value="code">
                        ```tsx
                            "use client"

                            import { Button, ToastAction, useToast } from "@holo/core"

                            export default function ToastWithAction() {
                              const { toast } = useToast()

                              return (
                                <Button
                                  variant="outline"
                                  onClick={() => {
                                    toast({
                                      title: "Uh oh! Something went wrong.",
                                      description: "There was a problem with your request.",
                                      action: <ToastAction altText="Try again">Try again</ToastAction>,
                                    })
                                  }}
                                >
                                  Show Toast
                                </Button>
                              )
                            }
                        ```
                    </Tab>
                </Tabs>
        </Step>
        <Step>
          ### With Title
                <Tabs items={["preview" , "code"]}>
                    <Tab value="preview">
                        <div className="py-16 flex items-center justify-center">
                            <ToastWithTitle />
                        </div>
                    </Tab>
                    <Tab value="code">
                        ```tsx
                        "use client"

                        import { Button, useToast } from "@holo/core"

                        export default function ToastWithTitle() {
                          const { toast } = useToast()

                          return (
                            <Button
                              variant="outline"
                              onClick={() => {
                                toast({
                                  title: "Uh oh! Something went wrong.",
                                  description: "There was a problem with your request.",
                                })
                              }}
                            >
                              Show Toast
                            </Button>
                          )
                        }
                        ```
                    </Tab>
                </Tabs>
        </Step>
    </Steps>
