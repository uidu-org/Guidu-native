{
  "name": "@uidu/breadcrumb-ui",
  "version": "0.1.3",
  "license": "ISC",
  "type": "module",
  "sideEffects": false,
  "exports": "./src/index.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.0.2",
    "@uidu/lib": "workspace:*",
    "class-variance-authority": "^0.7.0",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.55"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "scripts": {
    "build": "npx tsup src --dts",
    "build-dev": "npx tsup src --dts --watch",
    "versionPatch": "yarn version patch",
    "versionMinor": "yarn version minor",
    "versionMajor": "yarn version major",
    "npmPublish": "npm publish"
  }
}
