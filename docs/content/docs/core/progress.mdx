---
title: "Progress"
description: a great way to use Progress
---

import { Button } from "@holo/button";
import ProgressDemo from "../../../src/docs/demos/core/progress-demo.tsx";

    <div className="flex items-center">
        <Button variant="link" className="mr-4"><a className="!no-underline" href="" target="_blank">Storybook</a></Button>
        <Button variant="link" className="mr-4"><a className="!no-underline" href="https://www.npmjs.com/package/@holo/progress" target="_blank">@holo/progress</a></Button>
        <Button variant="link" className="mx-4"><a className="!no-underline"  href="https://github.com/uidu-org/holo/tree/main/packages/components/core/progress" target="_blank">Source</a></Button>
    </div>

## Progress

<ProgressDemo />

## Usage

    <Tabs items={["preview" , "code"]}>
        <Tab value="preview">
            <div className="py-16 flex items-center justify-center">
                <ProgressDemo />
            </div>
        </Tab>
        <Tab value="code">
            ```tsx
                "use client"

                import * as React from "react"

                import { Progress } from "@holo/core"

                export default function ProgressDemo() {
                  const [progress, setProgress] = React.useState(13)

                  React.useEffect(() => {
                    const timer = setTimeout(() => setProgress(66), 500)
                    return () => clearTimeout(timer)
                  }, [])

                  return <Progress value={progress} className="w-[60%]" />
                }

            ```
        </Tab>
    </Tabs>

## Installation

    <Tabs items={["npm" , "yarn"]}>
        <Tab value="npm">
            ```bash
                npm install @holo/progress
            ```
        </Tab>
        <Tab value="yarn">
            ```bash
                yarn add @holo/progress
            ```
        </Tab>
    </Tabs>

## Import

    <Tabs items={["generic" , "individual"]}>
        <Tab value="generic">
            ```tsx
                import { Progress } from "@holo/core"
            ```
        </Tab>
        <Tab value="individual">
            ```tsx
                import { Progress } from "@holo/progress"
            ```
        </Tab>
    </Tabs>
