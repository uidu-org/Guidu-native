---
title: "Alert"
description: Displays a message to interact with the user
---

import { Button } from "@holo/button";
import AlertDemo from "../../../src/docs/demos/core/alert-demo";
import AlertDestructive from "../../../src/docs/demos/core/alert-destructive";

    <div className="flex items-center">
        <Button variant="link" className="mr-4"><a className="!no-underline" href="" target="_blank">Storybook</a></Button>
        <Button variant="link" className="mr-4"><a className="!no-underline" href="https://www.npmjs.com/package/@holo/alert" target="_blank">@holo/alert</a></Button>
        <Button variant="link" className="mx-4"><a className="!no-underline"  href="https://github.com/uidu-org/holo/tree/main/packages/components/core/alert" target="_blank">Source</a></Button>
    </div>

## Alert

<AlertDemo />

## Usage

<Tabs items= {["preview", "code"]}>
  <Tab value="preview">
    <div className=" h-80 p-16 flex items-center justify-center">
     <AlertDemo />
    </div>

  </Tab>
  <Tab value="code">
  ```tsx
    "use client"

import { Terminal } from "lucide-react";

import { Alert, AlertDescription, AlertTitle } from "@holo/core";

export default function AlertDemo() {
  return (
    <Alert>
      <Terminal className="h-4 w-4" />
      <AlertTitle>Heads up!</AlertTitle>
      <AlertDescription>
        You can add components to your app using the cli.
      </AlertDescription>
    </Alert>
  );
}

````

</Tab>

</Tabs>

## Installation

<Tabs items={["npm", "yarn"]}>

<Tab value="npm" >

```bash
npm install @holo/alert
````

</Tab>

<Tab value={"yarn"}>```bash yarn add @holo/alert ```</Tab>

</Tabs>

## Import

<Tabs items={["generic" , "individual"]}>
<Tab value={"generic"}>
```tsx

import { Alert, AlertDescription, AlertTitle } from "@holo/core";

````
</Tab>

<Tab value={"individual"}>
```tsx

import {
  Alert,
  AlertDescription,
  AlertTitle,
} from "@holo/alert"

````

</Tab>
</Tabs>

## Variants

<Steps>

<Step>
### Destructive
<Tabs items={["preview", "code"]}>

<Tab value="preview">
    <div className=" h-80 p-16 flex items-center justify-center">
    <AlertDestructive />
    </div>

</Tab>
<Tab value="code">
```tsx
"use client"

import { AlertCircle } from "lucide-react";

import { Alert, AlertDescription, AlertTitle } from "@holo/core";

export default function AlertDestructive() {
  return (
    <Alert variant="destructive">
      <AlertCircle className="h-4 w-4" />
      <AlertTitle>Error</AlertTitle>
      <AlertDescription>
        Your session has expired. Please log in again.
      </AlertDescription>
    </Alert>
  )
}
```

</Tab>

</Tabs></Step>

</Steps>
